[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "markets-fuels-data-collector"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = ["pandas", "requests"]

[tool.setuptools]
packages = ["mf", "mf.extract"]
